pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Commands to build your project
                echo 'Building...'
            }
        }
        stage('Test') {
            steps {
                // Commands to test your project
                echo 'Testing...'
            }
        }
        stage('Deploy') {
            steps {
                // Commands to deploy your project
                echo 'Deploying...'
            }
        }
    }
}
